---
name: 项目功能总览
overview: 本项目为基于 Wails 的桌面「数据库工作台」：主界面管理环境与 MySQL/PostgreSQL 连接；独立 SQL 工作台窗口提供对象树、Monaco 编辑、查询执行与结果网格、DDL/建表/同步/结构查看及本地查询历史等能力；Go 侧负责连接池、可取消执行与数据同步等。
todos:
  - id: doc-main
    content: （可选）将本计划导出为项目内 README/文档章节
    status: pending
  - id: verify-bindings
    content: （维护时）改 Go API 后重新生成 frontend/bindings
    status: pending
isProject: false
---

# 数据库工作台 — 当前功能总览

## 架构与入口

- **桌面壳**：Wails 应用；前端为 React + Vite + Tailwind + shadcn/ui（`[frontend/src](frontend/src)`）。
- **路由**： `[App.tsx](frontend/src/App.tsx)`  根据 URL 参数切换视图：
  - 默认：`[MainMenu](frontend/src/views/MainMenu.tsx)`
  - `?studio=1&envId=<id>`：`[SqlStudio](frontend/src/views/SqlStudio.tsx)`（由 `[StudioWindow.OpenStudioWindow](frontend/bindings/changeme/studiowindow.ts)` 打开独立窗口）。

```mermaid
flowchart LR
  MainMenu[MainMenu]
  SqlStudio[SqlStudio]
  GoDB[DatabaseService Go]
  MainMenu -->|"打开 SQL 工作台"| SqlStudio
  SqlStudio --> GoDB
```



---

## 一、主菜单（环境与连接）

**文件**： `[frontend/src/views/MainMenu.tsx](frontend/src/views/MainMenu.tsx)` 


| 能力    | 说明                                                                                     |
| ----- | -------------------------------------------------------------------------------------- |
| 环境    | 添加 / 删除环境（分组）；删除前确认                                                                    |
| 连接    | 在指定环境下添加连接：名称、驱动（MySQL / PostgreSQL）、主机、端口、用户、密码、默认库                                   |
| 连通性   | 保存前 **TestConnectionParams**（`[database_service.go](database_service.go)` 临时 DSN Ping） |
| 连接列表  | 按环境展示；删除连接；统计环境与连接总数                                                                   |
| 打开工作台 | 按环境调用 **OpenStudioWindow(envId)** 打开 SQL 工作台窗口                                         |


**绑定**： `[workspaceservice.ts](frontend/bindings/changeme/workspaceservice.ts)` （List/Add/Remove Environment 与 Connection）。

---

## 二、SQL 工作台（核心）

**文件**： `[frontend/src/views/SqlStudio.tsx](frontend/src/views/SqlStudio.tsx)` 

### 2.1 连接与对象树

- 下拉选择当前 **数据库连接**（同环境下由 `[WorkspaceService.ListConnections](frontend/bindings/changeme/workspaceservice.ts)` 加载）。
- 列出 **数据库**（`[DatabaseService.ListDatabases](frontend/bindings/changeme/databaseservice.ts)`）；展开库后 **ListTables** 拉表名。
- 侧栏操作：**新建表**（`[CreateTableDialog](frontend/src/components/CreateTableDialog.tsx)`）、**DDL**（`[DdlDialog](frontend/src/components/DdlDialog.tsx)`）、表名 **预览**（生成 `SELECT * ... LIMIT`）、**结构**（`[TableStructureDialog](frontend/src/components/TableStructureDialog.tsx)`）、**编辑**（预填 ALTER 模板）。
- **上下文**：侧栏点表预览时写入当前标签的 `contextDb` / `contextTable`，工具栏显示「上下文表」；切换连接清空。

### 2.2 编辑器（Monaco）

- 多 **标签页**（新建、关闭、复制标签）；每标签独立 SQL 文本与结果状态。
- **执行**：整文档或**仅选中**语句；可选 **多条 SQL**（`[splitSqlStatements](frontend/src/lib/sqlSplit.ts)` 按分号拆分，忽略引号/反引号内分号）。
- **单条**：`[ExecuteSQL](frontend/bindings/changeme/databaseservice.ts)` + 当前 **activeDatabase** 作为默认库。
- **多条**：顺序 await 执行，汇总日志；最后一条若为网格结果则保留在结果区。
- **取消**：`[CancelRunningQuery](frontend/bindings/changeme/databaseservice.ts)`。
- **格式化**：`sql-formatter`（MySQL / PostgreSQL 方言）。
- **快捷键**：Ctrl+Enter / Ctrl+R 执行；Ctrl+Q 新标签；Ctrl+Shift+D 复制标签。
- **补全**：`loader.init()` 注册 **MySQL 关键字**（`[mysqlKeywords.ts](frontend/src/lib/mysqlKeywords.ts)`）+ 当前库/表；内置 SQL 关键字提示关闭以减少重复。

### 2.3 结果区（Glide Data Grid）

- 查询结果以 **DataEditor** 展示；主题与行高/列头可配置。
- **导出**：CSV（含 BOM）、JSON、**INSERT 语句**（`[exportInsert.ts](frontend/src/lib/exportInsert.ts)`）。
- **网格交互**：列宽拖动、列顺序拖动；新结果重置列布局；容器 `overflow-hidden` 裁剪画布边缘线。
- **右键**：复制（调用 grid `emit("copy")`）。
- 错误与多条执行的 **execLog** 分区展示。

### 2.4 其他对话框与本地能力

- **设置**：与工具栏联动的「多条 SQL」开关。
- **查询历史**：本地 **localStorage** 最近 100 条（`[queryHistory.ts](frontend/src/lib/queryHistory.ts)`），可填入当前标签、清空。
- **数据同步**：`[DataSyncDialog](frontend/src/components/DataSyncDialog.tsx)` → `[SyncTables](frontend/bindings/changeme/databaseservice.ts)`。
- **跨环境**：菜单中打开其他环境的 SQL 窗口（`OpenStudioWindow`）。

---

## 三、Go 侧数据库服务（后端能力摘要）

**文件**： `[database_service.go](database_service.go)` （与 `[databaseservice.ts](frontend/bindings/changeme/databaseservice.ts)` 绑定对应）


| 能力                                    | 要点                                                           |
| ------------------------------------- | ------------------------------------------------------------ |
| 连接池                                   | 按连接 ID 缓存；连接变更时剔除池（`evictPool`）                              |
| ExecuteSQL                            | 可 **context 取消**；默认库：MySQL USE / Postgres 切库；结果行数上限（如 50000） |
| CancelRunningQuery                    | 取消当前执行                                                       |
| ListDatabases / ListTables            | 元数据浏览                                                        |
| GetTableStructure / GetTableDDL       | 表结构与建表 DDL                                                   |
| SyncTables                            | 同驱动表级全量同步（源→目标，目标 TRUNCATE 后插入）                              |
| TestConnection / TestConnectionParams | 已保存连接 Ping / 表单参数 Ping                                       |


---

## 四、UI 与依赖（前端）

- **组件库**：shadcn（Button、Dialog、Dropdown、ScrollArea、Checkbox 等）— `[frontend/src/components/ui](frontend/src/components/ui)`。
- **编辑器**：`@monaco-editor/react`。
- **结果表**：`@glideapps/glide-data-grid`。

---

## 五、可文档化的扩展点（非新需求，仅现状边界）

- 主菜单与 SQL 工作台 **数据不通过 React Router**，工作台依赖 **独立窗口 + `envId` 查询参数**。
- 查询历史 **仅本机、按浏览器存储**，不与后端同步。
- PostgreSQL 与 MySQL 在侧栏预览、引号与 DDL 模板等处 **分支处理**（`driver` 判断）。

