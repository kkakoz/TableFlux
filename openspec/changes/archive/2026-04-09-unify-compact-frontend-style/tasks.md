## 1. 设计令牌与全局基线

- [x] 1.1 在 `frontend` 中确定紧凑间距与控件高度的承载方式（根级 CSS 变量与/或 Tailwind `@theme` 映射），并写下最小可点击高度等约束注释
- [x] 1.2 为默认按钮、次要按钮、图标按钮定义统一的小尺寸类名或组件变体，并在全局入口样式中挂载一次，避免重复魔法数

## 2. 界面收敛与迁移

- [x] 2.1 梳理 `App.tsx` 与高频 `components/*.css` 中的按钮、工具栏与表单控件，替换为新的紧凑类名或令牌
- [x] 2.2 对设置、对话框、顶栏/侧栏等用户常路径做一次视觉走查，修正明显不一致的间距或过大控件
- [x] 2.3 运行 `frontend` 生产构建（或项目文档中的等价命令）确认无样式构建错误与明显回归

## 3. Cursor 规则与文档对齐

- [x] 3.1 新增或更新 `.cursor/rules` 中「前端紧凑 UI」规则：变量/类名约定、修改全局样式时注意点、禁止随意新增互斥尺寸常量，并与代码中的命名一致
- [x] 3.2 对照 `specs/frontend-compact-ui/spec.md` 自查：密度一致、按钮规范、规则可发现性是否已满足
