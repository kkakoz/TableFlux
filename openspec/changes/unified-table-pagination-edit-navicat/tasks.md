## 1. 后端：按主键批量更新与预览 SQL

- [x] 1.1 在合适的 Database/Studio Service 中设计并实现**批量** `UpdateRowsByPK`（或等价命名）请求/响应结构，一次提交全部脏行；执行路径使用参数化 SQL 与标识符安全策略
- [x] 1.2 实现 PostgreSQL 与 MySQL 的 UPDATE 执行路径，返回明确错误信息供前端展示
- [x] 1.3 将方法挂到 Wails 暴露层并生成/更新前端 TypeScript 绑定类型
- [x] 1.4 实现与执行同源的 **UPDATE 预览文本**生成 API（或共享构建器），按方言对字符串字面量与标识符**正确转义/引用**，供 Modal 展示与复制
- [x] 1.5 覆盖 PostgreSQL 与 MySQL 的转义边界用例（如单引号、反斜杠），与手工或自动化用例对齐

## 2. 元数据与主键

- [x] 2.1 表标签路径：从现有表元数据获取主键列列表并传入结果网格或工具栏状态
- [x] 2.2 SQL 结果路径：定义何时可编辑（例如仅带表上下文或后端标注主键）；无主键时禁用执行并保持一致工具栏布局

## 3. 前端：统一结果区工具栏

- [x] 3.1 抽取或复用单表结果区工具栏布局，使 SQL 结果区采用相同结构（左上执行/取消，右上分页/刷新）
- [x] 3.2 为分页与刷新应用「较大」样式变体（CSS 变量或类名），与 `frontend-compact-ui` delta 对齐
- [x] 3.3 确认刷新、翻页与加载态在单表与 SQL 两种入口行为一致且无布局回归

## 4. 前端：网格编辑与 Navicat 式确认

- [x] 4.1 在 `VirtualResultGrid`（或等价组件）启用单元格编辑：`readonly`/`onCellEdited`，维护行快照与 **dirty 行集合**（提交范围：全部脏行）
- [x] 4.2 协调单击编辑与现有长文本双击/详情行为，避免手势冲突
- [x] 4.3 实现工具栏「取消」：从原始快照恢复未提交变更
- [x] 4.4 实现工具栏「执行」：拉取或生成预览 SQL 列表并打开 **Modal**（无脏行时禁用或提示）；Modal **右下角**「确定」触发后端批量更新、「取消」仅关闭 Modal
- [x] 4.5 Modal 内预览区支持复制（含全选/可选「复制全部」），布局符合「右下角确定/取消」

## 5. 规则与收尾

- [x] 5.1 在 Cursor 规则或项目约定中补充数据结果区工具栏尺寸变体说明（若规格要求可发现性）
- [x] 5.2 手工验证：工具栏位置与按钮尺寸、编辑/网格取消、**执行→Modal→确定/取消**、预览复制与转义、无主键禁用场景
